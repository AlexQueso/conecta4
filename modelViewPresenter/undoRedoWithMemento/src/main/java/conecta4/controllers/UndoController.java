package conecta4.controllers;

import conecta4.models.Game;
import conecta4.models.State;

public class UndoController extends Controller{
    UndoController(Game game, State state) {
        super(game, state);
    }
}
